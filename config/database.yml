<%
db_host    = ENV.fetch('DB_HOST', ENV.fetch('TCOM-PS_DB_1_PORT_5432_TCP_ADDR', 'localhost'))
db_port    = ENV['TCOM-PS_DB_1_PORT_5432_TCP_PORT'] || ENV['DB_PORT'] || 5432
require 'etc'
pool_size =
  # Web: max workers * max threads
  ENV.fetch('WEB_CONCURRENCY', 3).to_i * ENV.fetch('MAX_THREADS', 5).to_i +
  # ActiveJob Async max thread pool size
  Etc.nprocessors
ERB
%>

defaults: &defaults
  adapter: postgresql
  min_messages: WARNING
  pool: <%= pool_size %>
  host: <%= db_host %>
  port: <%= db_port %>
  username: <%= ENV.fetch('DB_USERNAME', 'tcom-ps_dev').inspect %>
  password: <%= ENV.fetch('DB_PASSWORD', "tcom-ps").inspect %>
  encoding: utf8

development:
  <<: *defaults
  database: tcom-ps_dev

test:
  <<: *defaults
  database: tcom-ps_test


production:
  <<: *defaults
  encoding: utf8
  min_messages: WARNING
  url: <%= ENV['DATABASE_URL'].inspect if ENV['DATABASE_URL'] %>
  database: <%= 'tcom-ps_prod' unless ENV['DATABASE_URL'] %>
